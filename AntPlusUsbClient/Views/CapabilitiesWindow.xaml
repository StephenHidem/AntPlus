<Window x:Class="AntPlusUsbClient.Views.CapabilitiesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AntPlusUsbClient.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:CapabilitiesViewModel}"
        xmlns:local="clr-namespace:AntPlusUsbClient.Views"
        mc:Ignorable="d"
        Title="Capabilities" Height="259" Width="225">
    <Grid Margin="10,0,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBlock Grid.Row="0" Text="{Binding MaxANTChannels, StringFormat='Max. ANT channel - {0}'}" />
            <TextBlock Grid.Row="1" Text="{Binding MaxNetworks, StringFormat='Max. Networks - {0}'}"/>
            <TextBlock Grid.Row="2" Text="{Binding MaxDataChannels, StringFormat='Max. Data Channels - {0}'}" />
        </StackPanel>
        <ListBox Grid.Row="1" ItemsSource="{Binding Capabilities}" ItemStringFormat="- {0}"></ListBox>
    </Grid>
</Window>
