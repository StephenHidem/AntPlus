<UserControl x:Class="WpfUsbStickApp.Controls.EllipticalControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfUsbStickApp.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:Background="White">
    <Grid>
        <GroupBox Header="Elliptical">
            <StackPanel>
                <TextBlock Text="{Binding Elliptical.StrideCount, StringFormat='Stride Count: {0}'}"/>
                <TextBlock Text="{Binding Elliptical.Cadence, StringFormat='Cadence: {0}'}"/>
                <TextBlock Text="{Binding Elliptical.InstantaneousPower, StringFormat='Instantaneous Power: {0}'}"/>
                <TextBlock Text="{Binding Elliptical.PosVerticalDistance, StringFormat='PosVerticalDistance: {0}'}"/>
                <TextBlock Text="{Binding State, StringFormat='State: {0}'}"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
